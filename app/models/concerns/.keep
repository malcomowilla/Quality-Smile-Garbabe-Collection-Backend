  default from: 'mailtrap@aitechsent.net'

    def customer_code(customer)
      @customer = customer
      mail(
        to: @customer.email,
        subject: 'Your Customer Code',
        template_name: 'customer_code'  # Ensure you have a corresponding view template
      )
    end